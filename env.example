# Supabase 設定
# Supabase Dashboard > Project Settings > API から取得

# Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# anon public key（クライアントサイドで使用可能）
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# service_role key（サーバーサイドのみ、絶対にクライアントに公開しないこと）
# SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# 自動注文実行APIの認証用シークレット（Vercel Cron Jobsから呼び出す際に使用）
# ランダムな文字列を設定してください（例: openssl rand -hex 32）
# AUTO_ORDER_SECRET=your-random-secret-string

# 本番環境のサイトURL（メール内のリンクに使用）
# 本番環境: 必ず設定してください（例: https://bento-order-app-blond.vercel.app）
# 開発環境: 設定不要（デフォルトで http://localhost:3000 が使用されます）
# 重要: この環境変数を設定しないと、パスワードリセットメールのリンクがローカルURLになる場合があります
# また、Supabase Dashboard > Authentication > URL Configuration > Site URL も本番環境のURLに設定してください
# NEXT_PUBLIC_SITE_URL=https://bento-order-app-blond.vercel.app

# データベース接続URL（PostgreSQL接続文字列）
# Supabase Dashboard > Project Settings > Database > Connection string > Transaction から取得
# Transaction connection (ポート6543)を使用することで、接続プールを活用しパフォーマンスが向上します
# 形式: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true
# 注意: 
# - この環境変数はサーバーサイドでのみ使用可能です（クライアントサイドでは使用できません）
# - Transaction connection (6543)を使用することを強く推奨します（Direct connection (5432)より高速）
# DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:6543/postgres?pgbouncer=true